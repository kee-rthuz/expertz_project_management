

// // wrking code now 1

// import React from 'react';
// import BidCard from './BidCard'; // Import the BidCard component
// import styles from './BidCardList.module.css'
// export default function BidCardList({ bids, onCardClick, setShowOverlay }) {
//   const sortedBids = Array.isArray(bids)
//     ? [...bids].sort((a, b) => b.bidAmount - a.bidAmount)
//     : [];

//   return (
//     <div>
//       {sortedBids.map((bid) => (
//         <BidCard key={bid._id} bid={bid} onCardClick={onCardClick} setShowOverlay={setShowOverlay} />
//         ))}
//     </div>
//   );
// }











